name: cloud_text_to_speech
description: Single interface to Google, Microsoft and Amazon Text-To-Speech.
version: 2.3.2
repository: https://github.com/markokosticdev/cloud_text_to_speech_flutter

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  json_annotation: ^4.8.1
  build_runner: ^2.4.9
  locale_names: ^1.1.1
  xml: ^6.3.0
  sigv4:
    git:
      ref: 0570fb1e8d912d10dab0f6ef7befbcd861149cc8
      url: git@github.com:curefit/sigv4.git


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.1

topics:
  - tts
  - text-to-speech
  - google-cloud
  - azure
  - aws